{
  "name": "join-info-evry",
  "version": "1.0.0",
  "description": "Membership application system for Asso Info Evry",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro build && wrangler dev",
    "deploy": "astro build && wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ui": "vitest --ui",
    "db:init": "wrangler d1 execute join-db --local --file=./db/schema.sql",
    "db:seed": "wrangler d1 execute join-db --local --file=./db/seed.sql",
    "db:migrate": "wrangler d1 execute join-db --file=./db/schema.sql"
  },
  "keywords": ["astro", "cloudflare", "workers", "d1", "membership"],
  "author": "Asso Info Evry",
  "license": "MIT",
  "dependencies": {
    "astro": "^5.16.2",
    "@astrojs/cloudflare": "^12.6.12"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "~0.10.11",
    "vitest": "~4.0.14",
    "wrangler": "^4.51.0"
  }
}
